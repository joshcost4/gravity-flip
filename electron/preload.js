const { contextBridge } = require('electron');

// Placeholder preload. The app does not currently need any Electron<->renderer bridge.
// Keeping this file enables future expansion safely.
contextBridge;

